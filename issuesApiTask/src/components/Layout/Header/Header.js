import styled from "styled-components";

function Header() {

    return(
        <S.Wrapper>
            
        </S.Wrapper>
    );
}
export default Header;

const Wrapper = styled.div`

`

const S = {
    Wrapper,
}